#include <TamiyaTank.h>

void ctrlServo () {
	Servo *servo = &(sys.barrelVerital);
	
	if (servo->output > servo->para.neutro) {
		
	} else if (servo->output < servo->para.neutro) {
		
	} else {
		
	}
	
}
